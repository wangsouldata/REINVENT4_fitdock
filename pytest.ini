[pytest]
markers =
    integration: mark a test as an integration test.
    needs_gpu: mark a test as requiring a hardware accelerator (cuda/mps/xpu); skipped automatically when the requested device is unavailable.
addopts = --device cpu
